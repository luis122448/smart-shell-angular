services:
  app:
    container_name: smart-shell-angular
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    environment:
      API_URL: $API_URL
      HEALTH_CHECK_URL: $HEALTH_CHECK_URL
    ports:
      - "${SMART_SHELL_ANGULAR_PORT}:80"
    networks:
      - smart-shell-net
    restart: unless-stopped

networks:
  smart-shell-net:
    external: true
